# ✅ قائمة الفحص النهائي قبل النشر — BANCO @ `1f860f0`

> نتيجة الفحص الشامل 2026-07-05 — كل بند اتفحص فعلياً بأمر قابل للتكرار.

## 1) الكود والبناء (كله أخضر — مثبَت)

| الفحص | الأمر | النتيجة |
|---|---|---|
| السويت الخلفي الكامل (53 ملف) | `vitest run` (TZ=UTC + DATABASE_URL) | **265 ✓ / 3 متخطى / 0 فشل** |
| typecheck كل الأسطح السبعة | `pnpm run typecheck` (الجذر) | **0 أخطاء** |
| أيقونات SVG أندرويد (كل اسم مستخدَم في التطبيق) | `node --test tests/icons.test.mjs` | **6/6** |
| نظافة الريبو | `git status` | صفر تعديلات غير مقصودة، HEAD=boom/main |

## 2) نظافة المنتج (اتفحصت بالـgrep — نظيفة)

- [x] **صفر** `console.log` في كود تطبيق الموبايل.
- [x] **صفر** TODO/FIXME/HACK متبقية في الموبايل والـAPI.
- [x] **صفر** علامات AI ظاهرة للمستخدم (لا "Generated with"، لا نصوص placeholder).
- [x] إضافات Replit في vite **مقفولة على التطوير فقط** (`NODE_ENV !== "production"` + dynamic import) — لا تدخل بناء الإنتاج. (تعليقات `// @replit` في مكوّنات ui مجرد تعليقات غير مرئية.)
- [x] **الصفحة الوهمية القديمة اتشالت**: لاندنج B-OOM الآن صفحة رسمية بكل العناوين الرئيسية (التطبيق + ماركت + الأدمن) بروابط من env — لا رابط Replit قديم.
- [x] صفر أسرار/داتا في git (dumps + attached_assets متشالة من التتبع).

## 3) هوية المتاجر (جاهزة في app.json)

- [x] الاسم: **BANCO OOM** · الإصدار: **1.0.0**
- [x] iOS bundle: `com.bancooom.app` · Android package: `com.bancooom.app`
- [x] الأيقونة + scheme + صلاحيات بحدّها الأدنى (CAMERA فقط).
- [x] بروفايلات EAS: production + preview (APK) + development.

## 4) دورات الحياة (مختبرة في السويت)

- [x] الإعلان: إنشاء → نشط → مباع/أرشفة → إعادة تدوير (bump) — لكل الأقسام (سيارات/عقارات/صناعي) بفلسفة "لا تمنع تجارة".
- [x] الإيجار بنموذجيه: مفروش يومي = حجز فندقي كامل (توفّر/تداخل/تأكيد/رفض/إلغاء/تحرير تواريخ + إشعار المالك) · طويل المدى = إعلان عادي. **8/8 اختبارات**.
- [x] المستخدم: تسجيل → نوع حساب → توثيق → أعمال B2B.
- [x] الماسنجر: إرسال تفاؤلي/إعادة محاولة/ردود/تفاعلات/صور + إيموجي احترافي.
- [x] الخرائط: `GET /v1/search/map` عناقيد + فلاتر إيجار/بيع دائمة الظهور.
- [x] المساعد الذكي: موديل fallback صحيح + ذكاء سيارات حقيقي.

## 5) المتبقي — بيئة فقط (مش كود) — يفعَّل عند التسليم

- [ ] مفاتيح Clerk الإنتاجية (بدونها الـAPI يرد 500 على المصادقة — سلوك متوقع).
- [ ] DATABASE_URL إنتاجي + مفاتيح OpenAI/Resend (+ Paymob لاحقاً).
- [ ] دومينات الويب النهائية → تحديث `VITE_*` في اللاندنج.
- [ ] `git tag v1.0.0` ثم بناء EAS من الوسم (الدليل: STORE_PUBLISHING_GUIDE.md).

## 6) حدود الفحص الحالي (شفافية كاملة)

الاختبار تحت **حِمل إنتاجي حقيقي** (آلاف المستخدمين المتزامنين) لا يمكن إثباته
من هذا الجهاز — الضمانات هنا: بوابات كود كاملة + اختبارات سلوكية شاملة +
rate-limiters مُركّبة على كل المسارات + فهارس DB على مسارات القراءة الساخنة.
أول Internal Testing على المتجر هو خطوة الضغط الواقعية الأولى — الدليل يشرح
تفعيلها قبل الإطلاق العام.
