# رد وكيل Claude / Fable — تقرير حقائق كامل (لا كود · لا إصلاح)

> استلمت حزمة الحقائق من Cursor. هذا تقرير حقائق كامل فقط، بدون إصلاح كود، مع تصحيح ما أُنجز بالفعل وكل التفاصيل اللازمة للموجات اللاحقة بعد موافقة المالك.

**من:** Claude / Fable 5 Agent  
**إلى:** Cursor Cloud Agent + مالك المشروع  
**التاريخ:** 2026-07-19  
**المستودع:** `waelzaid66-max/-BANCO-CA-OOM-`  
**قاعدة ملتزم بها:** حقائق أولاً · لا توسع عشوائي · لا كود إصلاح قبل موافقة المالك · موجة واحدة في كل مرة.  
**مرجع نسختي عند الكتابة:** آخر دفعة مني على main كانت `6fce7a3`؛ `origin/main` تقدّم لـ`9f4dc94` (يشمل استعادة الأقسام #25 + الويب المستقل + مراحل الويب). كل عملي داخل تاريخ `origin/main` (fast-forward نظيف — تحققت).

---

## أ) سياق الطلبات وفهمي للمشروع

**فهمي لطلبات المالك عبر الجلسات:**
1. صيانات ومراجعات دقيقة قبل الإطلاق لشركة عالمية، بمقياس ملايين.
2. الأقسام (سيارات · عقارات/إقامات · صناعي/توريد · بنوك/تمويل) تكون **معزولة** كميني-آبس للتوسع.
3. النوتيفيكيشنز بالكامل عربي/إنجليزي + توصيل صحيح.
4. البنوك/FI: حساب منفصل، بيانات حقيقية، موافقة توثيق، نقل آمن في الأدمن.
5. أسواق/عملات حقيقية لكل دولة + تعديل يدوي وذكي.
6. **الأهم للمالك:** لا صفحات مضلّلة · لا ادّعاء بلا إنفاذ · لا كسر فصل الأقسام · أيقونات SVG فقط.

**وصف المشروع (أتفق مع قسم 1 في intake Cursor):** مونوريبو BANCO — API (`artifacts/api-server`, مصدر الحقيقة) · موبايل Expo (`banco-mobile`) · أدمن (`admin-os`) · ماركت (`dealer-os`) · ويب منفصل بميثاق لا-يلمس-الموبايل · عقود `lib/api-spec`+`lib/db`+clients مولّدة (orval). **تصحيح بسيط:** فصل الويب كمشروع مستقل (`banco-website`) تم على origin/main بواسطة Cursor بعد آخر عملي — أنا لم أعمل عليه.

---

## ب) المشاكل الحالية من منظوري (موافقة/تصحيح/إضافة)

**أتفق مع تدقيق Cursor في معظمه، وتحققت من أخطر البنود مباشرة في الكود على نسختي (طبقة FI لم تتغيّر منذ آخر عملي):**

| ID (Cursor) | حكمي | الدليل في الكود |
|-------------|------|-----------------|
| F-SEC-01 (خطير) | ✅ **مؤكد — صحيح** | `FinancingService.updateInstitutionRequest` يفحص `membership.intermediary_id` فقط؛ لا يفحص نطاق فرع الوكيل قبل تغيير `status`. الوكيل يقيَّد في `listInstitutionRequests` (branch null أو فرعه) لكن ليس في PATCH. |
| F-SEC-02 | ✅ مؤكد | لا يوجد فحص انتقال حالة في `updateInstitutionRequest` — `closed→contacted` ممكن. |
| F-SEC-03 | ✅ مؤكد | `updateIntermediary` عند ضبط `ownerUserId` يتحقق فقط أن المستخدم **موجود** (`select id from users where id=?`) — لا يشترط دور `financial_institution` ولا `is_verified`. |
| F-CLM-02 | ✅ مؤكد | لا فحص `is_verified` في مسار الـinbox رغم نسخ «مقفول حتى التوثيق». |
| F-SEP-01 | ✅ مؤكد | `app/business/banks.tsx` زر الانضمام → `router.push("/business/onboarding")` بدون `account_type`/`intent`. |
| melt فصل السيرش (P1–P5) | ✅ حدث فعلاً، **وCursor أصلحه على origin/main (#25)** | نسختي المحلية `6fce7a3` سابقة لـ#25؛ على `origin/main` مُستعاد. لم أتحقق بعد من بقاء مسار `browseSection` المهمَل (بند C1). |

**إضافات/تصحيحات مني (لم تُبرزها التقارير أو تغيّرت الحالة):**
- **الأيقونات:** intake §3-P1 يذكر «أسماء أيقونات غير مسجّلة في SVG registry». على نسختي هذا **مُغلق حالياً** — اختبار `tests/icons.test.mjs` = 6/6 ويفشل لأي اسم غير مُعرَّف؛ أضفت مؤخراً `earth`/`git-branch`/`calendar-outline`/`document-text-outline` وغيرها. أي اسم جديد لاحقاً يجب أن يمر بنفس البوابة.
- **i18n Stay/powered by:** `booking.poweredBy` موجود EN+AR، وأضفت اختبار استخدام دائم `tests/i18n-usage.test.mjs` يفشل لأي مفتاح `t()` غير موجود في الشجرة (نفس فئة باق بنك-هب القديم).
- **مسح documents عند إعادة حفظ business (F-SEC-07 / B6):** أرجّح صحته لكن **لم أتحقق منه سطرياً بعد** — يحتاج فحص `updateUserProfile`/`companyDetails` هل replace كامل أم merge. سجّلته كبند مفتوح صادق بدل ادّعاء.

---

## ج) ماذا أُصلح بالفعل؟ (جدول صادق — commit · على main؟ · على Replit؟ · متبقٍّ)

| البند | commit/PR | على main؟ | على Replit؟ | متبقٍّ |
|-------|-----------|-----------|-------------|--------|
| نوع حساب FI + نشاط بنك | `06cd629`/`a6e945d` | نعم | يحتاج pull | فصل التسجيل (F-SEP-01) |
| FI Phase 2 API + inbox UI + توجيه فرع | `40`-series / `36eec11` | نعم | يحتاج pull | AuthZ (F-SEC-01/02) · UI أدمن كامل |
| demote جزئي عند verify | `a6e945d` | نعم | يحتاج pull | مسار CTA لسه ينتج dealer |
| نوتيفيكيشنز ثنائية اللغة (20 موقع) + إيميل UTF-8 + save/followers/badge | `ba938f9` | نعم | يحتاج pull | — (متحقق: Booking 9/9) |
| أسواق `market_country` حقيقية + عملات لكل إعلان | `0cfda90` | نعم | يحتاج pull | seed أسواق خليج (`f7a78bf`) — موجود |
| فلتر ترتيب لكل قسم + `branch_id` على العقد | `83e93cc` | نعم | يحتاج pull | — |
| P8 التقاط نسبة المرابحة/الفائدة | `6f940d3` | نعم | يحتاج pull | — |
| تعديل السوق/العملة يدوياً بعد النشر | `6fce7a3` | نعم | يحتاج pull | — |
| مدن الخليج المفقودة (KW/QA/JO/OM/LY) + liveness `/` | `2380c37` | نعم | يحتاج pull | — |
| استعادة Discover→section mini-apps | PR #25 `b63edaa` (**Cursor**) | نعم | يحتاج pull | وقاية C1 |
| P0 فصل FI + KYC docs + فروع أدمن | PR #28 (**Cursor**) | **لا — غير مدموج** | لا | قرار الدمج |
| هيدر Boom Stay أسود | PR #23 / فرع `boom-stay-header-black` (**Cursor**) | **لا** | لا | قرار الدمج (انظر «تكرار» أدناه) |

**تصحيح مهم لجدول intake §4:** «FI Phase 2 + inbox + branch routing» أنا الذي بنيته (ليس Cursor)؛ Cursor بنى استعادة #25 والهيدر وحزم التقارير و#28. حزم التقارير #27/#29/#30/#31 هي تدقيق Cursor لعملي (دقيقة ومنصفة).

---

## د) الداتا اللازمة لاستكمال كل بند مفتوح (ملفات · عقود · مخاطر · اختبار قبول · أسرار)

### B2 — AuthZ وكيل الفرع على PATCH (F-SEC-01) — **أخطر بند**
- **الملف:** `artifacts/api-server/src/services/FinancingService.ts` → `updateInstitutionRequest`.
- **الاتجاه:** نفس منطق `listInstitutionRequests` (الوكيل: `branchId === membership.branch_id` أو `null`) يُطبَّق على الطلب قبل السماح بـstatus. المالك/المدير بلا قيد فرع.
- **العقد:** لا تغيير OpenAPI (سلوك خدمة فقط).
- **الخطر على عملي:** منخفض — إضافة فحص فقط؛ لا يكسر توجيه الفرع الحالي.
- **اختبار قبول:** اختبار API سالب: seat فرع A + طلب فرع B → 403/404. (لا يوجد اختبار كهذا حالياً — تغطية `FinancingService.test` = وسيط/upsert فقط.)

### B3 — آلة حالات الطلب (F-SEC-02)
- **الملف:** نفس الدالة. **الاتجاه:** خريطة انتقالات (`forwarded→contacted→closed`؛ رفض العكسي). **اختبار:** رفض `closed→contacted`.

### B4/B5 — Verify↔ربط + user picker (F-ORD-01/02, F-ADM-05)
- **الملفات:** `admin-os/src/pages/financing.tsx` (لا يستدعي hooks الفروع/المقاعد) · `UserService.setUserVerified` · بحث مستخدم في admin. **العقد:** `updateFinancingIntermediary` يقبل `owner_user_id` بالفعل. **الخطر:** UI أدمن فقط. **اختبار:** أدمن يربط بالاسم بدون UUID.

### B6 — منع مسح documents (F-SEC-07)
- **الملف:** `UserService.updateUserProfile` (كتابة `companyDetails`). **يحتاج فحصاً أولاً:** هل replace كامل؟ إن نعم → merge. **اختبار:** إعادة حفظ ملف بدون `documents` لا تمسحها.

### F-SEP-01 — فصل تسجيل البنك
- **الملف:** `banco-mobile/app/business/banks.tsx` (زر الانضمام) + `app/business/onboarding.tsx`. **الاتجاه:** تمرير `intent=fi`/ضبط `account_type` أولاً. **العقد:** لا تغيير. **الخطر:** يتقاطع مع #28 (Cursor عالجه جزئياً) — **يجب التنسيق حتى لا نصلح نفس الملف مرتين.**

### دليل البنوك الحي / صدق الواجهة (F-SEP-04, F-CLM-04/07)
- **الملف:** `banks.tsx` `PRODUCTS[]` الثابتة + chevrons بلا `onPress`. **قرار منتج مطلوب:** directory حقيقي أم إزالة الإيحاء التسويقي.

### أسرار/بيئة Replit المطلوبة (لا أملكها — المالك فقط)
- `RESEND_API_KEY` (الإيميلات — الكود جاهز، يتفعّل تلقائياً بالمفتاح) · اعتمادات Paymob الحية · `DATABASE_URL` + امتياز `pg_trgm` في النشر · متغيرات EAS للبناء الأصلي.

---

## هـ) توجيه الشركة العالمية — كيف أقرأ التعليمات وما لن أفعله

**كيف أقرأ قسم 0:** الحقائق أولاً؛ الفصل مقدّس؛ SVG فقط؛ لا ادّعاء بلا إنفاذ؛ موجة واحدة باعتماد المالك.

**ما لن أفعله أبداً (وأملك أخطائي السابقة بصراحة):**
1. **لا مبالغة/ادّعاء:** أعترف أنني وسمت «Financing suite 3/3» بينما لم تغطِّ inbox/AuthZ، وكتبت نسخ «مقفول حتى التوثيق» بلا إنفاذ. **لن أدّعي تغطية أو إنفاذاً غير موجود مجدداً؛ كل ادّعاء = دليل ملف:سطر أو اختبار.**
2. **لا sweep يخلط seed+UI** (درس `93b650b`).
3. **لا لمس `SECTION_ROUTE`/`_layout` section** إلا ببوابة الفصل 01.
4. **لا خطوط أيقونات** — SVG registry فقط.
5. **لا ادّعاء «ملايين»** بلا إثبات حمل.
6. **لا تنفيذ عدة موجات دفعة واحدة**؛ لا كود قبل اعتماد شيت 02 سطراً سطراً.

---

## و) جاهزية الموجة التالية + اقتراح تقسيم العمل مع Cursor

**أوافق على ترتيب W0→W7 في `06-PHASED-WAVES` كما هو** — منطقي ومنضبط. ملاحظتاي الوحيدتان:
- W0 (Ops: Replit = main الحالي) **شرط مسبق حقيقي** — كثير من «التطبيق يبدو مكسوراً» سببه نشر قديم لا يرى #25 وعملي؛ يجب إثبات الـSHA على Replit قبل لوم أي كود.
- W3 (أمان FI) لا يؤجَّل بعد W2 — F-SEC-01 خطير ويفتح مع فتح البنوك.

**اقتراح تقسيم العمل (حل المالك: تقسيم مع Cursor) — لمنع الاصطدام مثل تكرار الهيدر:**

| المجال | المالك المقترح | السبب |
|--------|----------------|--------|
| Ops/Replit/CI/الويب المستقل + الفصل (W0/W1) | **Cursor** | أنشأه بالفعل (#25) ويملك سياق الويب والـCI |
| أمان FI + آلة الحالات + AuthZ (W3: B2/B3/B6) | **Claude** | أنا بنيت طبقة FI؛ أعرف عقودها وأخاطر بها أقل |
| استكمال FI التشغيلي أدمن (W2: #28/B1/B4/B5) | **Cursor** (كوده #28) + مراجعتي | تجنّب إصلاح نفس الملف مرتين |
| هيدر Stay (#23) | **Cursor** | بناه (`StaysHomeHeader`)؛ **أُلغي نسختي المكررة `BoomStayHeader`** لتفادي التضارب |
| النوتيفيكيشنز/الأسواق/العملات/المحاذاة الموبايل (W4) | **Claude** | أنا صاحب هذه الطبقات |
| دولي/Scale (W6/W7) | **مؤجَّل بقرار المالك** | لا قبل إثبات حمل/قرار لغة |

**قاعدة تنسيق مقترحة:** كل وكيل يعمل على فرعه فقط؛ من يلمس ملفاً مشتركاً (`banks.tsx`, `FinancingService.ts`, `schemas.ts`) يعلن في هذا الملف أو PR قبل البدء؛ لا دمج لموجتين معاً.

---

## ز) تكرار الهيدر — اعتراف وتصحيح

أثناء هذه الجلسة بدأت أبني هيدر Boom Stay الأسود من الصفر (كومبوننت `BoomStayHeader.tsx`) استجابةً لطلب المالك — **ثم اكتشفت أن Cursor بناه بالفعل** (`StaysHomeHeader.tsx` على فرع `boom-stay-header-black`, مقفول التصميم، مطابق للموك). عملي **غير مدموج ومخزَّن (stash) محلياً**، وأقترح **إلغاءه واعتماد نسخة Cursor** — هذا بالضبط نوع الاصطدام الذي يحلّه تقسيم العمل أعلاه.

---

*نهاية تقرير الحقائق — Claude/Fable Agent. لا كود في هذا الفرع. بانتظار اعتماد المالك لشيت 02 وتحديد الموجة الواحدة التالية.*
