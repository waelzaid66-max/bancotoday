# Claude ACK — JOINT-ARCHITECTURE A1–A7

**من:** Claude / Fable 5 · **إلى:** Cursor (قائد الجودة) + المالك · **التاريخ:** 2026-07-19  
**النوع:** توقيع اتفاق — لا كود.

## أوافق على JOINT-ARCHITECTURE A1–A7

بلا اعتراض. الترتيب معماري سليم ويمنع تكرار الأخطاء.

| بند | توقيعي |
|-----|--------|
| A1 W0→W1→W2→W3→W4 بلا قفز | ✅ |
| A2 W1 = Cursor (CI أخضر #32) | ✅ — راجعتُه (ملف المراجعة المرفق) |
| A3 W2 = دمج #28 + مراجعتي للعقد | ✅ |
| A4 W3 = Claude بعد `Start W3` · قاعدة main بعد W2 | ✅ — لا كود FI مني قبل الجملة |
| A5 ملفات التصادم المحرّمة بلا إعلان | ✅ — لن ألمس `FinancingService`/`banks.tsx`/`onboarding.tsx`/`schemas.ts`/`SearchDiscover.tsx` بلا handoff |
| A6 الإخفاق يُكتب · لا تجميل | ✅ — ملتزم (اعترفتُ بمبالغاتي في TASK-001) |
| A7 لا مسح ميزات · SVG فقط | ✅ |

## قرأتُ
- `JOINT-ARCHITECTURE-EXECUTION-PLAN-AR.md`
- `CAPABILITY-SPLIT-AND-HONESTY-PROTOCOL-AR.md`
- `FULL-PICTURE-SAFE-PATH-MASTER-AR.md` (اللوحة الأم — G0→G6 + MUST-KEEP + تصنيف A–F)
- PR #32 (W1) / commit `0700a77` + `section-miniapp-guard.test.mjs`

## 3 نقاط صدق أضيفها للسجل (بدون تغيير الاتفاق)

1. **تناقض الهيدر — قرار للمالك:** المالك أرانى موك **B-OOM STAY الأسود** كهدف وطلب بناءه (وأنا بدأتُ `BoomStayHeader`)، لكن اللوحة الأم تصنّف الهيدر الأسود **تنفيذاً غلط (B)** وMUST-KEEP = **الهيرو الوردي**. **ألغيتُ نسختي المكررة** والتزمتُ بالوردي حتى يحسم المالك صراحة. لا أنفّذ أياً منهما بلا كلمة مالك.
2. **W4 sort-chip بتاعي مُتبنّى:** فرع `cursor/w4-section-sort-chip` = نفس commit `2f7e24f` بتاعي — نقلتُ الترتيب من رابع أيقونة في الهيدر إلى شريط الفلاتر (يفكّ الزحمة، يبقى في كل قسم). تعاون لا تصادم.
3. **MUST-KEEP ملتزم به:** لن ألمس `SECTION_ROUTE`/الميني-آبس · لا ENTER · لا هيدر أسود · لا `iconBtn<12` · لا `topPad=67` · Banks ليست قسم Search · لا بيانات مختلقة.

## التزامي في هذا المسار
- W3 (أمان FI) ملكي — بعد `Start W3` فقط، على قاعدة `main` بعد W2، PR واحد، اختبارات سالبة.
- أي لمس لملف مشترك = إعلان في `audit/handoff/` أولاً.
- «لم أتحقق» صريحة حيثما لم أفتح ملفاً.

— Claude / Fable 5
