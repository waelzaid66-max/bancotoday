# تقسيم القدرات + بروتوكول الصدق (أمر المالك 2026-07-19)

**أمر المالك حرفياً (ملخّص ملزم):**  
قسّم الشغل حسب القدرات · أقصى قوة على ضبط الموبايل الشكل والبايط · ابدأ بقوة مع Claude · فهم وسيطرة وصيانات حقيقية · **ممنوع انحراف/خلط** · **ممنوع الفلاتر/الشكل يدخلوا في بعض** · ممنوع الهَرْج · **حقائق وصدق كامل فقط** · الإخفاق يُعلَن ويُصلَح · **ممنوع الكذب** · فحص الشغل أكثر من مرة · كلّم Claude وخُذ منه وأكمِل معه.

---

## 1) تقسيم القدرات (مثبّت — لا سرقة مسار)

| المسار | المالك التنفيذي | أمثلة |
|--------|-----------------|--------|
| **Ops / Replit / CI / إثبات SHA** | **Cursor (X)** | W0: pull Replit = `main` · healthz |
| **فصل الأقسام · منع ذوبان الفلاتر · CI guard** | **Cursor (X)** | W1: قطع `onBrowseSection` · حماية `SECTION_ROUTE` |
| **دمج #28 FI P0 UI/KYC/فروع أدمن** | **Cursor (X)** + مراجعة عقد Claude | W2 |
| **هيدر Stay #23 · محاذاة شكل موبايل (بروفايل RTL جزئي، Banks chevron، i18n)** | **Cursor (X)** | W4/W5 — موبايل UI بأقصى دقة |
| **أمان FI (AuthZ فرع · state machine · owner role · docs merge · isActive)** | **Claude (C)** | W3 فقط بعد `Start W3` |
| **نوتيفيكيشن / أسواق / عملات / عقود API العميقة** | **Claude (C)** | بعد اعتماد المالك |
| **قرارات منتج · أسرار · اعتماد صفوف الشيت · أمر الموجات** | **Owner (O)** | بوابة |

**قاعدة اصطدام:** من يلمس `banks.tsx` / `FinancingService.ts` / `schemas.ts` / `onboarding.tsx` / `SearchDiscover.tsx` يعلن في `audit/handoff/` قبل الدفع.

---

## 2) بروتوكول الصدق (لا كذب · لا تخمين)

1. كل ادّعاء = `commit` أو `ملف:سطر` أو «لم أتحقق».  
2. فحص **مرتين** على الأقل قبل إعلان «مُصلَح» (قراءة كود + اختبار/CI إن وُجد).  
3. الإخفاق يُكتب في اليدoff بصراحة (مثل RTL جزئي، #28 غير مدموج).  
4. ممنوع خلط موجتين أو خلط فلتر Search العام مع ميني-آب القسم.  
5. ممنوع مسح ميزات — تقوية فقط.  
6. أيقونات Android = SVG registry فقط.

---

## 3) حالة الأخذ من Claude (الآن)

| ملف Claude | الحالة بعد فحص Cursor المزدوج |
|------------|-------------------------------|
| `CLAUDE-RESPONSE-FULL-FACTS-AR.md` | ✅ مأخوذ |
| `CLAUDE-MASTER-FEATURE-LIFECYCLE-SHEET-AR.md` | ✅ مأخوذ |
| `CLAUDE-SURGICAL-FOLLOWUP-PROFILE-MINIAPP-AR.md` (`48142ad`) | ✅ مأخوذ · انظر `CURSOR-VERIFY-PASS-CLAUDE-TASK001-AR.md` |

---

## 4) ترتيب التنفيذ الفعلي بعد أمر «ابتدي»

```
W0  Owner+Cursor إرشاد: Replit pull (لا كود من السحابة على جهازك)
W1  Cursor الآن: وقاية الفصل — قطع سلك browseSection + CI guard
W2  Cursor: #28 بعد اعتمادك الصريح للدمج
W3  Claude: بعد الجملة الحرفية Start W3 فقط
W4  Cursor: محاذاة موبايل (بما فيها coverActions end/RTL) — بلا خلط مع FI
```

---

## 5) جملة لـ Claude (سيطرة مشتركة)

> أخذتُ TASK-001 follow-up. معظم الحقائق **PASS** بعد فحص مزدوج.  
> Cursor يبدأ **W1** (فصل فلاتر) في مساره.  
> أنتَ تبقى على وضع الحقائق/الاستعداد لـ W3 — **لا كود FI** حتى `Start W3`.  
> أي خلاف على دليل: اكتب في القناة · نصحّح سوياً بلا هَرْج.

— Cursor
